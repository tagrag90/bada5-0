# 개선된 작업 프로세스

**작성일**: 2025-01-16  
**목적**: 작업물 소실 방지 및 근본적 문제 해결  
**기반**: 근본 원인 분석 결과

---

## 🚫 절대 금지 사항

1. **브랜치 생성/사용 금지**
   - 브랜치에서 작업 후 원복이 안 된 사례 있음
   - 모든 작업은 main 브랜치에서 직접 수행
   - 백업도 브랜치가 아닌 커밋 또는 stash 사용

2. **되돌리기 명령어 금지** (사용자 명시적 지시 없이)
   - `git checkout -- .`
   - `git restore .`
   - `git reset --hard`
   - `git clean -fdx`

---

## ✅ 개선된 작업 프로세스

### 1. 작업 시작 전

**필수 확인**:
```bash
# 1. 현재 브랜치 확인 (main이어야 함)
git branch
# 출력: * main

# 2. Git 상태 확인
git status

# 3. 최근 커밋 확인
git log --oneline -5
```

**백업 생성 (권장)**:
```bash
# 현재 상태를 커밋으로 백업 (브랜치 사용 금지)
git add .
git commit -m "백업: [작업 내용] 시작 전 - $(date +%Y%m%d_%H%M%S)"
```

---

### 2. 작업 중

**원칙**:
- 작은 단위로 나누어 작업
- 각 변경마다 검증
- 문제 발생 시 즉시 중단

**검증 체크리스트 (각 변경 후)**:
- [ ] 빌드 성공 (`npm run build`)
- [ ] 타입 체크 통과 (`npx tsc --noEmit`)
- [ ] 브라우저에서 실제 동작 확인
- [ ] 기존 기능 여전히 작동
- [ ] 에러 로그 없음

---

### 3. 작업 완료 시

**필수 절차**:
```bash
# 1. 최종 검증
npm run build
npx tsc --noEmit

# 2. Git 상태 확인
git status

# 3. 변경사항 커밋
git add .
git commit -m "feat: [작업 내용]"

# 4. 서버 재시작 (필요 시)
# pkill -f "next dev" && npm run dev
```

---

### 4. 문제 발생 시

**대응 순서**:
1. **즉시 중단** - 더 이상 수정하지 않음
2. **문제 정확히 파악**
   - 어떤 기능이 작동하지 않는가?
   - 에러 메시지는 무엇인가?
   - 브라우저 콘솔에 에러가 있는가?
3. **현재 상태 확인**
   ```bash
   git status
   git branch  # main인지 확인
   git log --oneline -5
   ```
4. **해결 방안 제시** (사용자에게)
   - 문제 원인 분석
   - 해결 방안 제시
   - 리스크 설명
5. **사용자 확인 후 실행**
   - 승인 후에만 수정
   - 작은 단위로 나누어 실행
   - 각 단계마다 검증

**롤백 (사용자 명시적 지시 시에만)**:
```bash
# 방법 1: 마지막 커밋으로 복구
git reset --hard HEAD~1

# 방법 2: stash로 복구
git stash pop

# 방법 3: 특정 커밋으로 복구
git reset --hard <commit-hash>
```

**주의**: 롤백 전에 반드시 사용자 확인 필요

---

## 🔍 문제 해결 프로세스

### 문제 보고 수신 시

**1단계: 정확한 문제 파악**
- 어떤 기능이 작동하지 않는가?
- 언제부터 문제가 발생했는가?
- 에러 메시지가 있는가? (정확한 메시지)
- 브라우저 콘솔에 에러가 있는가?
- 네트워크 탭에 실패한 요청이 있는가?
- 이전에는 정상 작동했는가?

**2단계: 현재 상태 분석**
```bash
# Git 상태
git status
git branch
git log --oneline -10

# 빌드 상태
npm run build

# 타입 체크
npx tsc --noEmit
```

**3단계: 근본 원인 분석**
- 왜 문제가 발생했는가?
- 어떤 변경이 원인인가?
- 기존 코드와의 충돌은 없는가?

**4단계: 해결 방안 제시**
- 여러 옵션 제시
- 각 옵션의 리스크 설명
- 권장 방안 제시

**5단계: 사용자 확인**
- 승인 후 실행
- 작은 단위로 나누어 실행
- 각 단계마다 검증

---

## 📋 검증 체크리스트

### 코드 레벨 검증
- [ ] 타입 체크 통과 (`npx tsc --noEmit`)
- [ ] 린터 에러 없음 (`npm run lint`)
- [ ] 빌드 성공 (`npm run build`)

### 런타임 검증
- [ ] 브라우저에서 실제 동작 확인
- [ ] 에러 로그 없음 (콘솔, 네트워크)
- [ ] 기존 기능 여전히 작동 (회귀 테스트)

### 데이터 레벨 검증
- [ ] 데이터베이스 무결성 확인
- [ ] 마이그레이션 상태 확인
- [ ] API 응답 정상

---

## 🎯 핵심 원칙

1. **이해 → 분석 → 계획 → 실행 → 검증**
   - 이해 없이 수정하지 않음
   - 분석 없이 계획하지 않음
   - 계획 없이 실행하지 않음
   - 검증 없이 완료하지 않음

2. **작은 단위로 나누기**
   - 한 번에 하나의 파일만 수정
   - 한 번에 하나의 기능만 추가
   - 각 변경마다 검증

3. **확인 후 실행**
   - 사용자 확인 없이 대규모 변경 금지
   - 문제 해결 방안 제시 후 승인 대기
   - 롤백도 사용자 확인 후 실행

4. **브랜치 사용 금지**
   - 모든 작업은 main에서 직접 수행
   - 백업도 브랜치가 아닌 커밋 또는 stash 사용

---

## 📝 작업 완료 시 체크리스트

- [ ] 빌드 성공
- [ ] 타입 체크 통과
- [ ] 브라우저에서 실제 동작 확인
- [ ] 기존 기능 회귀 테스트
- [ ] 에러 로그 없음
- [ ] Git 커밋 완료
- [ ] 서버 재시작 (필요 시)

---

**이 프로세스는 근본 원인 분석을 기반으로 작성되었습니다. 모든 작업은 이 프로세스를 따라야 합니다.**

---

**작성자**: AI Assistant  
**검토자**: Junseo Park  
**최종 업데이트**: 2025-01-16

